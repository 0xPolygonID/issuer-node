export type UserContext = {
  UserDID: string;
  password: string;
  username: string;
  // setUserDetails: (username: string, password: string, UserDID: string) => void;
};
export type User = {
  UserDID: string;
  password: string;
  username: string;
};
